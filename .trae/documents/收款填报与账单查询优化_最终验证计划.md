# 收款填报与账单查询优化 — 最终验证计划

## 一、背景

本会话上一轮已完成 5 项需求的全部代码改动，并通过后端 58 个测试与前端浏览器验证步骤 1-6。唯一遗留：步骤 7（设置客户业务负责人 → 生成账单 → 验证账单 follow_up_user_id 取自 customer.business_owner_id）因浏览器中选中租户无客户数据，改为 API 验证，进行到创建客户 ID=15（business_owner_id=23）后中断。

本计划聚焦于**完成剩余验证 + 收尾**，不涉及任何新功能改动。

## 二、当前状态确认（Phase 1 探索结果）

### 代码改动已就位

| 需求 | 文件 | 状态 |
|------|------|------|
| 1. 收款填报去除款项性质 | [PaymentSubmit.vue](file:///e:/code/ai_code/payclip/frontend/src/views/PaymentSubmit.vue) | ✅ 已删除"款项性质"字段，form 初始值移除 usage_type |
| 2. 选择截图按钮换样式 | [PaymentSubmit.vue](file:///e:/code/ai_code/payclip/frontend/src/views/PaymentSubmit.vue#L61-L65) | ✅ 改为 `el-button text type="primary"` + Upload 图标 |
| 3. 账单管理查询功能 | [BillList.vue](file:///e:/code/ai_code/payclip/frontend/src/views/BillList.vue#L3-L25) + [bills.py](file:///e:/code/ai_code/payclip/backend/app/routers/bills.py#L50-L71) | ✅ 客户/状态/业务负责人/年/月 筛选 + 后端 follow_up_user_id 参数 |
| 4. 收款核对渠道中文化 | [PaymentVerify.vue](file:///e:/code/ai_code/payclip/frontend/src/views/PaymentVerify.vue#L17) | ✅ channelText 函数映射 bank→银行转账 等 |
| 5. 客户业务负责人 + 账单快照 | [CustomerList.vue](file:///e:/code/ai_code/payclip/frontend/src/views/CustomerList.vue#L27-L29) + [customers.py](file:///e:/code/ai_code/payclip/backend/app/routers/customers.py#L37-L56) + [bills.py generate_bills](file:///e:/code/ai_code/payclip/backend/app/routers/bills.py#L150-L161) + [models](file:///e:/code/ai_code/payclip/backend/app/models/__init__.py#L110) + [main.py 迁移](file:///e:/code/ai_code/payclip/backend/app/main.py#L54-L69) | ✅ Customer 新增 business_owner_id；generate_bills 优先取 customer.business_owner_id，fallback 到 sub.service_owner_id |

### 服务运行状态

- 后端 Uvicorn：`http://127.0.0.1:8000` 运行中（job-ffe5021da904413ea786fbce01f556ae），迁移已自动执行
- 前端 Vite：`http://localhost:5173` 运行中（job-c4ccce46cca24b19a3208a1627379d97）

### 已完成的前置数据

- 租户 9（演示工作室）有用户：demo_admin(22)、demo_emp(23)、demo_emp2(24)
- 已创建测试客户 ID=15，business_owner_id=23（演示员工）

## 三、剩余验证步骤

### 步骤 A：跑后端全量测试（回归确认）

```bash
cd e:\code\ai_code\payclip\backend
python -m pytest tests/ -v
```

**期望**：全部测试通过（之前是 58 个），无回归。

### 步骤 B：API 验证账单业务负责人快照逻辑

用已获取的 token + `X-Tenant-Id: 9` headers 完成 4 步：

1. **创建长期业务（subscription）**
   ```
   POST /api/subscriptions
   Body: { "company_id": 15, "service_type": "代账", "billing_period": "monthly",
           "monthly_fee": 2000, "service_owner_id": 24, "start_date": "2025-07-01" }
   ```
   *注意 service_owner_id 故意用 24，以区别于客户的 business_owner_id=23，验证优先级*

2. **生成账单**
   ```
   POST /api/bills/generate
   Body: { "company_id": 15, "year": 2025, "month": 7 }
   ```

3. **查询账单并验证**
   ```
   GET /api/bills?company_id=15&year=2025&month=7
   ```
   **期望**：返回账单的 `follow_up_user_id = 23`（来自 customer.business_owner_id），`follow_up_user_name = "演示员工"`，**而非** 24（service_owner_id）

4. **清理（可选）**：删除测试 subscription 和 bill，避免污染数据

### 步骤 C：补充验证（可选，时间允许时做）

- 反向验证：把客户 15 的 business_owner_id 设为 null，再生成 2025-08 账单，验证 follow_up_user_id 回退到 24（subscription.service_owner_id）

## 四、执行约束

- **不修改任何业务代码**（5 个需求已实现完毕）
- 若步骤 A 测试失败 → 分析是否为新需求引入的回归，最小化修复
- 若步骤 B 验证失败 → 排查 bills.py generate_bills 逻辑，必要时修复
- 严格遵守用户规则：中文回答、总结不超 100 字、不随意更改未要求的地方

## 五、完成标志

- 后端全量测试通过
- API 验证账单 follow_up_user_id = customer.business_owner_id（23）确认成功
- 向用户返回 ≤100 字的中文总结
